%Luis Monroy

%% Option1
clc,clear,close all

%x=11;
x=5;
%x=-2;

if (x>10)
    y=log(x)
elseif (x>=0) & (x<=10)
    y=x.^(1/2)
else 
    y=exp(x)-1
end

%given y=ln(x) for x>10, y=x.^(1/2) for (x>=0) & (x<=10), and y=e.^(x)-1
%for x<0. Compute y. Test program for x=11, x=5, x=-2.

%% Option2
clc,clear,close all

x=11;
%x=5;
%x=-2;

if (x>10)
    y=log(x)
elseif (x>=0) & (x<=10)
    y=x.^(1/2)
elseif (x<0)
    y=exp(x)-1
end

%given y=ln(x) for x>10, y=x.^(1/2) for (x>=0) & (x<=10), and y=e.^(x)-1
%for x<0. Compute y. Test program for x=11, x=5, x=-2.

%using "elseif" is safer than using "else"
